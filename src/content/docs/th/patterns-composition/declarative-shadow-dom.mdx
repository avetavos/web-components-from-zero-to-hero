---
title: 'Declarative Shadow DOM'
description: 'ใช้ attribute shadowrootmode บน element template เพื่อแนบ shadow DOM โดยไม่ต้องใช้ JavaScript — เหมาะอย่างยิ่งสำหรับ server-side rendering'
sidebar:
  order: 5
---

import LivePreview from '../../../../components/LivePreview.tsx';
import Tip from '../../../../components/Tip.astro';
import Quiz from '../../../../components/Quiz.tsx';
import ProgressTracker from '../../../../components/ProgressTracker.tsx';

## Declarative Shadow DOM คืออะไร

Declarative Shadow DOM (DSD) ช่วยให้คุณแนบ shadow root เข้ากับ element โดยใช้แค่ HTML โดยไม่ต้องใช้ JavaScript เลย สิ่งนี้เหมาะอย่างยิ่งสำหรับ server-side rendering (SSR) ที่ shadow tree สามารถถูกรวมไว้ใน HTML response เริ่มต้น และ browser จะแนบ shadow root ให้ก่อนที่ JavaScript ใด ๆ จะทำงาน

## attribute shadowrootmode

การวาง `<template shadowrootmode="open">` (หรือ `"closed"`) เป็น child โดยตรงของ element จะทำให้ browser แนบ shadow root เข้ากับ element แม่โดยอัตโนมัติ และย้ายเนื้อหาของ template เข้าไปในนั้น

```html
<my-card>
  <template shadowrootmode="open">
    <style>
      p { color: #0070f3; font-weight: bold; }
    </style>
    <p>This content lives in the shadow root.</p>
    <slot></slot>
  </template>
  Light DOM content here (goes into the slot)
</my-card>
```

แท็ก `<template>` จะถูก browser ใช้งานไปและถูกแทนที่ด้วย shadow root ที่ถูกแนบ ไม่ต้องใช้ JavaScript เลย element จะมี shadow root ทันทีตอน parse

## เรื่องราวของการ hydration

เมื่อ JavaScript โหลดขึ้นมา `customElements.define('my-card', MyCard)` จะอัปเกรด element หาก class เรียก `attachShadow` ใน constructor ของตัวเอง browser จะข้ามการสร้าง shadow root ตัวที่สอง (เพราะมีอยู่แล้ว) และนำตัวที่มีอยู่กลับมาใช้ใหม่ นั่นหมายความว่าเนื้อหาที่ถูก render จากเซิร์ฟเวอร์จะถูกรักษาไว้ — คุณสมบัตินี้สำคัญมากสำหรับ SSR hydration

```js
class MyCard extends HTMLElement {
  connectedCallback() {
    // Shadow root already attached declaratively — shadowRoot is available
    const shadow = this.shadowRoot; // not null
    if (shadow) {
      // hydrate: add event listeners, update state, etc.
    }
  }
}
customElements.define('my-card', MyCard);
```

## หมายเหตุเรื่องการรองรับของ browser

Declarative Shadow DOM (attribute มาตรฐาน `shadowrootmode`) รองรับใน Chrome 111+, Edge 111+, Safari 16.4+ และ Firefox 123+ — ข้าม browser หลักทั้งหมดตั้งแต่ต้นปี 2024 เมื่อ Firefox เริ่มรองรับ (ก่อนหน้านั้นมี attribute `shadowroot` แบบไม่เป็นมาตรฐานใน Chrome 90–110 เท่านั้น ซึ่งถูกลบไปแล้วและแทนที่ด้วย `shadowrootmode` — อย่าสับสนกับ tutorial เก่า) ใน browser รุ่นเก่า attribute `<template shadowrootmode>` จะไม่เป็นที่รู้จัก และ template จะยังคงเฉื่อย (inert) อยู่ ให้ใส่ polyfill สำหรับ browser รุ่นเก่า:

```js
// Polyfill for older browsers
document.querySelectorAll('template[shadowrootmode]').forEach(function(t) {
  t.parentElement.attachShadow({ mode: t.getAttribute('shadowrootmode') })
    .appendChild(t.content.cloneNode(true));
  t.remove();
});
```

method คู่หนึ่งที่เกี่ยวข้องกันคือ `shadowRoot.getHTML()` และ `shadowRoot.setHTMLUnsafe()` ซึ่งรองรับข้าม browser หลัก (Baseline) ตั้งแต่เดือนกันยายน 2024 `getHTML()` แปลง shadow root กลับเป็น HTML string — เป็นด้าน "อ่าน" ที่คู่กับ declarative shadow DOM — ส่วน `setHTMLUnsafe()` ช่วยให้คุณ parse string ที่มี markup `<template shadowrootmode>` ให้กลายเป็น shadow tree จริงได้ ซึ่ง `innerHTML` เพียงอย่างเดียวทำไม่ได้

## เดโมที่รันได้

LivePreview ด้านล่างแสดง `<dsd-card>` ที่มี `<template shadowrootmode="open">` อยู่ใน HTML แล้ว slot จะรับข้อความจาก light DOM ไม่ต้องใช้ JavaScript เพื่อให้ shadow แนบ

export const dsdHtml = `<dsd-card>
  <template shadowrootmode="open">
    <style>
      :host { display: block; border: 2px solid #0070f3; border-radius: 8px; padding: 1rem; }
      h3 { margin: 0 0 0.5rem; color: #0070f3; }
    </style>
    <h3>Shadow-rendered heading</h3>
    <slot></slot>
  </template>
  <p>This paragraph lives in light DOM, projected via the slot.</p>
</dsd-card>`;

export const dsdJs = `var out = document.createElement('p');
out.style.marginTop = '0.5rem';
out.style.fontSize = '0.8rem';
out.style.color = '#666';
out.textContent = 'No customElements.define needed — shadow root attached by HTML parser.';
document.body.appendChild(out);`;

<LivePreview client:visible html={dsdHtml} js={dsdJs} />

<Tip title="Great for SSR">
Declarative Shadow DOM โดดเด่นในสถานการณ์ server-side rendering ส่ง shadow tree ไปเป็น HTML แล้ว browser จะ render ให้ทันที ก่อนที่ JavaScript ใด ๆ จะถูกดาวน์โหลด, parse หรือ execute จากนั้น JavaScript hydration จึงค่อยเพิ่มความสามารถในการโต้ตอบโดยไม่ต้อง render ซ้ำ
</Tip>

## ข้อแลกเปลี่ยน

| ตัวเลือก | Benefit | Cost |
|---|---|---|
| Declarative Shadow DOM (`<template shadowrootmode>`) | shadow root มีอยู่ตั้งแต่ parse HTML เลย ไม่ต้องรอ JavaScript โหลดเพื่อ render — สำคัญมากสำหรับ SSR และ first paint | server ต้อง duplicate markup ของ template ไว้ในทุก instance ของ component ทำให้ payload HTML ใหญ่ขึ้นตามจำนวน component บนหน้า |
| attachShadow แบบ JavaScript ปกติ | markup ฝั่ง server เล็กกว่า ไม่มี template ซ้ำ | ต้องรอ JavaScript โหลดและรันก่อน ระหว่างนั้นผู้ใช้อาจเห็นเนื้อหาไม่มีสไตล์ (FOUC) หรือไม่เห็นเนื้อหาเลย |

## ข้อผิดพลาดที่พบบ่อย

- **สับสนว่าต้องเรียก `attachShadow` ซ้ำใน `connectedCallback`** — ถ้า shadow root ถูกแนบแบบ declarative ไว้แล้ว browser จะข้ามให้เฉย ๆ ไม่ throw error แต่ถ้า logic ของคุณ assume ว่าต้องสร้างเองเสมอ อาจเขียน initialization ผิดที่และเผลอทับ shadow root ที่มีอยู่แล้ว
- **ไม่เช็คการรองรับของ browser ก่อน deploy** — browser รุ่นเก่าที่ไม่รองรับ DSD จะ render `<template>` เป็น element ธรรมดาที่มองเห็นได้ ทำให้ markup ภายในหลุดออกมาแสดงตรง ๆ กลางหน้า
- **ไม่ตรวจสอบ `this.shadowRoot` ก่อน hydrate ฝั่ง client** — ถ้า client-side code สร้าง shadow root ใหม่ทับของเดิมโดยไม่เช็คก่อน จะทำให้เนื้อหาที่ render จาก server หายไปและต้อง render ซ้ำโดยไม่จำเป็น เสียประโยชน์ของ DSD ไปเปล่า ๆ

> 💡 ตัวอย่างจากของจริง
>
> **Lit SSR (`@lit-labs/ssr`)** — render component เป็น Declarative Shadow DOM บนฝั่ง server แล้วส่ง HTML ที่มี shadow tree ครบไปให้ browser ทันที ลด time-to-first-paint และไม่ต้อง render ซ้ำตอน hydrate ([lit.dev/docs/ssr](https://lit.dev/docs/ssr/overview/))
>
> **Adobe Spectrum Web Components** — ออกแบบให้ compatible กับ SSR และ Declarative Shadow DOM เพื่อให้ design system ขนาดใหญ่ render ได้เร็วตั้งแต่ first paint โดยไม่ต้องรอ JavaScript hydrate ([opensource.adobe.com](https://opensource.adobe.com/spectrum-web-components/))

export const quizDsd = [
  {
    q: 'HTML attribute ใดบน element template ที่กระตุ้นให้เกิด Declarative Shadow DOM?',
    options: [
      'shadow-root',
      'shadowrootmode',
      'attachshadow',
      'shadowmode',
    ],
    answer: 1,
  },
  {
    q: 'browser แนบ shadow root ใน Declarative Shadow DOM เมื่อใด?',
    options: [
      'เมื่อ customElements.define ถูกเรียก',
      'ระหว่างการ parse HTML ก่อนที่ JavaScript จะทำงาน',
      'เมื่อ connectedCallback ทำงาน',
      'เมื่อ DOMContentLoaded ทำงาน',
    ],
    answer: 1,
  },
  {
    q: 'เกิดอะไรขึ้นกับ this.shadowRoot ใน class ของ custom element เมื่อมี declarative shadow root อยู่แล้ว?',
    options: [
      'เป็น null — คุณต้องเรียก attachShadow อีกครั้ง',
      'คืน declarative shadow root ที่ถูกแนบไว้แล้ว',
      'โยน InvalidStateError',
      'เป็น undefined จนกว่าจะถึง connectedCallback',
    ],
    answer: 1,
  },
];

<Quiz client:visible id="patterns-composition/declarative-shadow-dom" questions={quizDsd} />

<ProgressTracker client:visible id="patterns-composition/declarative-shadow-dom" />
